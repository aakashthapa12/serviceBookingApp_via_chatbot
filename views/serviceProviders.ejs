
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Service Providers</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(to right, #ffecd2 0%, #fcb69f 100%);
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .container {
            background: #ffffff;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 40px; /* Increased padding for better spacing */
            text-align: center;
            max-width: 600px; /* Decreased max-width for a smaller container */
            width: 100%;
            margin: 20px;
        }

        h1 {
            font-size: 2em;
            margin-bottom: 30px; /* Increased margin-bottom for better spacing */
            color: #444444;
        }

        h2 {
            font-size: 1.5em;
            margin-bottom: 20px; /* Increased margin-bottom for better spacing */
            color: #444444;
        }

        input[type="email"] {
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #ccc;
            box-sizing: border-box;
            margin-bottom: 20px; /* Increased margin-bottom for better spacing */
            width: calc(100% - 20px); /* Adjusted width */
            max-width: 300px; /* Added max-width */
        }

        button, a {
            display: inline-block;
            background: linear-gradient(45deg, #ff7e5f, #feb47b);
            color: #ffffff;
            padding: 10px 20px;
            border-radius: 5px;
            border: none;
            cursor: pointer;
            transition: background 0.3s, color 0.3s;
            text-decoration: none;
            margin-bottom: 20px; /* Increased margin-bottom for better spacing */
        }

        button:hover, a:hover {
            background: linear-gradient(45deg, #feb47b, #ff7e5f);
        }

        .section {
            margin-bottom: 40px; /* Increased margin-bottom for better spacing */
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="section">
            <h1>Service Providers</h1>
        </div>

        <div class="section">
            <h2>View your Services by Providing Email</h2>
            <form action="/service-provider/services" method="get">
                <input type="email" name="email" placeholder="Enter provider email" required>
                <button type="submit">View Services</button>
            </form>
        </div>

        <div class="section">
            <h2>Add a New Service</h2>
            <a href="/service-provider/add-service">Add Service</a>
        </div>
    </div>
</body>
</html>
